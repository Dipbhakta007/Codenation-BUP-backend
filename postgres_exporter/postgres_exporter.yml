auth_modules:
  codenationDB: # Set this to any name you want
    type: userpass
    userpass:
      username: ict
      password: bupict2016
    options:
      # options become key=value parameters of the DSN
      sslmode: disable